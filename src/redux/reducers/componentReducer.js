const defaultState = {
  header: null,
  body: null,
  footer: null
};

const componentReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default componentReducer;
